# getrandom 0.3 (pulled transitively via v_utils -> ustr -> ahash) needs the wasm_js backend on wasm.
# Paired with the `wasm_js` feature in [target.wasm32-unknown-unknown.dependencies] in Cargo.toml.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
